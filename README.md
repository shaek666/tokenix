# tokenix
A handwritten compiler built in C#, implementing a full compilation pipeline from lexical analysis and parsing to binding, lowering, and code emission. Designed with clean APIs for parsing and type checking, making the language toolable from the ground up.
